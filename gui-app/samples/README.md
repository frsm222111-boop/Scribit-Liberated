# Sample SVG Files

Place your sample SVG files in this directory. They will automatically appear in the Draw tab's sample selection.

The app will display all `.svg` files found in this directory as clickable samples.
